# apiVersion: v1
# kind: Pod
# metadata:
#   name: "{{ include "learners-helm.fullname" . }}-test-connection"
#   labels:
#     {{- include "learners-helm.labels" . | nindent 4 }}
#   annotations:
#     "helm.sh/hook": test
# spec:
#   containers:
#     - name: wget
#       image: busybox
#       command: ['wget']
#       args: ['{{ include "learners-helm.fullname" . }}:{{ .Values.service.port }}']
#   restartPolicy: Never
