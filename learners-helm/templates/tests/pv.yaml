apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.dind-jenkins.claimName }}
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 1Gi
  storageClassName: hostpath
  hostPath:
    path: {{ .Values.dind-jenkins.path }}
